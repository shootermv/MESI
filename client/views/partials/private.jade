.alert.alert-info click on the status to change it
ul.list-group
	li.list-group-item(ng-repeat="task in tasks | orderBy:'status.id':false")
		span {{task.summary}}
		status-picker.statuspckr(ng-model="task.status.name") {{task.status.name}}
